# Q2 连续站位搜索冻结计划

本文件及实现先提交，随后才运行新 adaptive 搜索。历史数据只作基线与回归。
输出根目录 `J:/2026B_experiments/q2_continuous_20260911/run`，禁止覆盖已有运行。

固定主例：p=(0,0)、theta=30°；首测评分多边形直接读取历史 q2.json，不重算。
lambda=.2 m/s，T=|q-p|/5+5。方向评分复用连续角度包络：72初始区间、
gap<=.01m、最多20000次分裂；所有后验用FP64 all-pairs。near 单独保守处理，
后验直径不超过10m。评分上下值是数值包络，非带FP误差证明的严格上下界。

模式：M0=legacy_213+旧Csafe；M1=adaptive_safe_region+旧Csafe；
M2=adaptive_safe_region+Q0；M3=adaptive_safe_region+Q5（仅本例完整环扇形精确）。
一般裁剪精确Qvis不实现。四组使用同一内层评分、同一权重；旧15情景独立回归。

* 所有模式种子包含旧213；新增粗网格在首测局部坐标 x=0:50:1500、
  y=-1000:40:1000。M1/M2/M3同一生成过程，只改变安全域。
* top-K=5，按J上值、坐标排序，贪心保留相距至少50m的多个起点，保留此前全部已评分可行点参与选优。
* 层级0粗网格+全局边界采样，后续步长20、10、5、2、1m；初始邻域尺度50m。
  每个父点局部偏移为[-floor(prev/step),...,floor(prev/step)]*step，二维笛卡尔积。
* 粗边界按圆周弧长<=50m采样，并包含圆圆交点。每级对top5向每个约束圆径向投影，
  沿圆弧在±prev范围内以step加密。边界点向固定旧赢家内移1e-4m，防止浮点贴边误判。
* 候选生成不构成认证。所有入选评分点须经过独立有理数区间圆盘 verifier。
  M0/M1额外验证Q5，以避免仅依赖旧浮点外包的认证解释。
* 达到1m后停止，不提前停止；收敛判断用最终2→1m的 |delta_J|<.05m
  且 |delta_q|<2m。无收敛不加跑更细网格、不回头调参。
* 采用门槛：全部证书通过、无几何异常、所有内层gap<=.01m；相对M0
  J上值降低>=1%，且M0下值减新上值>.1m；最终层满足上述收敛。
  本轮只有固定主例，没有额外holdout，不将固定例改进推广为任意首测通用最优。
  若通过，只作为固定主例人工审核建议；不自动更新主配置、正文或推送。

原始输出：每级候选CSV（父点、坐标、安全模式、认证、上下值、T/J、排名、边界距离），
refinement.csv、所有唯一站位的角度叶区间gzip JSON、配置/代码hash/运行时间JSON。
4进程本地几何计算，跨模式完全相同的站位可缓存，记录唯一计算量与模式引用量。
不调用求解器、模拟器、官方演练或正式接口。正式测试启动次数=0。

## 最新补充约束（首次adaptive运行前冻结）

任务名 Q2_RECEPTION_AWARE_OUTER_REFINEMENT。B=M1新增高精度评分上限2000，
C=M2+M3共享新增评分上限2000（不隐瞒两种扩大域的总计算预算），总墙钟45分钟。
每组预留150次供半步及最终细角审核；达到1850时停止该组新增空间扩张。
旧候选评分及跨组缓存命中另计；所有精度.001的复查也占对应组预算。
M2保留M1全部通过认证的池；M3保留M1/M2全部通过认证的池，避免丢弃旧优点。
预算到达允许BUDGET_LIMITED；不保证各组能遍历完所有预定层级。
候选池保留不劣只适用于已评估点，不构成连续全局最优保证。

运行前用旧213中的索引0、106、176测量内层开销，保存PILOT_COST.json，不用新点调参。
最终每组原表赢家附近局部±1m以.5m半步网格和相同边界方向复查；原表赢家及
半步赢家重新序列化并通过硬验证，重做.001m内层审核。半步改进独立报告，不伪装
已在1m层收敛。M0不改变候选池，只复核旧赢家。图形只显示真实候选点与标明用途的边界。
需要的plan.json与environment.json由程序在搜索前生成，配置与本提交源码绑定。
完成后打包、仅提交本轮Q2文件，停在人工审核点。最新要求覆盖此前自动推送授权。

验证：至少18类请求中的测试；密集数值搜索仅找反例；回归旧213评分与已知卡尺反例。
结果表从原始CSV独立重建，冻结后写审计与结果；Q1/Q3/Q4及旧结果hash必须不变。
