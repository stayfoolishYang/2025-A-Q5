# Stage 05 Pre-Solve QA

项目：`CUMCM2026_A_DRYING`。QA版本：`A26-05-v1`。审查入口：Stage04 `A26-04-v1`；授权局部修订后的权威规格：`A26-04-v2`，输入版本仍为`A26-INPUT-v1`。日期：2026-09-10。本文为求解前审查与实施交接，不是求解器验证、模拟结果或比赛论文。

当前文档状态：Stage05审查、局部修订及受影响复核已完成。**Gate05：CONDITIONAL_PASS**；B/C规格均为READY_FOR_IMPLEMENTATION，当前没有开放的数学实施阻塞。完整方案的人审确认仍为NOT_CONFIRMED；Stage06–08、正式PDE及模拟结果均NOT_RUN。

## 1. QA Scope

审查同一个B_EMPIRICAL_RADIAL（Baseline/Implementation Primary）及C_AXISYMMETRIC_REFERENCE（二维参考），保留P的KEEP_IN_RESERVE与H不启用。入口以实际文件为准：Stage03 v1、Stage04 v1、数据清单、状态及Stage04证据均已读取/对照；同会话未改的上游内容依据哈希复用，Stage01/02不重新通读。原件在需要核对输入范围、模板与物性出处时定位，七个原件哈希及所有工作簿结构已复查。适用根AGENTS及Stage05完整规则已读，未发现项目嵌套AGENTS，未读取Stage06–08规则。

用户本轮允许数学、量纲、孤立标量/符号检查和局部Stage04修订。后续又明确要求子代理审计和“不要保持economy直接开启最高模式”，故当前项目为PERFORMANCE；这只改变本项目执行策略，不授权正式PDE、源工程、结果表或下一阶段。

### Execution / Reasoning Metadata

| 字段 | 实际记录 |
| --- | --- |
| Operating Mode | 入口ECONOMY；用户追加后PERFORMANCE，项目模式及预算字段同步更新 |
| Primary Model / Reasoning Level | 父会话实际后端身份/档位UNVERIFIED；核心子代理显式请求gpt-6-astra/ultra，调度已接受 |
| Reviewer Used | YES；一个独立数值审计子代理显式请求gpt-5.6-sol/high，调度已接受 |
| Reviewer Role / Review Status | NUMERICAL_REVIEWER / COMPLETED；含MMS补丁定向回读；核心数学复核同样COMPLETED；父代理逐项裁定见§3/21和evidence |
| Task Type / Complexity / Decision Risk | REVIEW及CORE_REASONING / HIGH / HIGH；非线性时间控制、事件误差传播和守恒接口 |
| Routing Source | 用户本轮两条追加授权＋根AGENTS模型偏好；pool默认文件未修改，不冒充自动启用 |
| Ownership | 子代理只读返回发现；父代理独占权威文档/状态写入并做整合复核；无递归代理或Best-of-N |
| Evidence | SPEC_RECHECKED；TEST_ONLY_EXECUTED；FORMAL_NUMERICAL_VALIDATION_NOT_RUN三层分列 |

调度接受和审计返回是本轮可见证据；不把配置值当作独立测得的远端后端身份。核心复核负责连续屏障、重建和事件条件性；独立审计负责BDF2/FVM/诊断/输出，父代理负责原件/依赖、孤立检查、修订与Gate。

## 2. Entry-Gate Status

入口Gate01–04均CONDITIONAL_PASS，Gate05–08为NOT_RUN；baseline=primary_candidate=B，challenger=C，current_best_verified=null、model_frozen=false。入口Stage04 SHA-256为`e13edd5c81ce07ebdd034068e3390aca282e8209de1810ff2d243bdcd56f5780`，与其证据一致。

Stage05任务授权没有登记为“Stage04全文已外部人工验收”。审查时将经验交换、有效热模型、轴向近似、收缩运动、长期输入和数值精度条件逐项作为活跃目标；区分规格缺陷、已声明假设和执行后才能获得的结论。

Stage04 v1已完整复制至`workspace/stale/04_model_specification_rev001.md`，非空且哈希与入口一致；只归档实际修改的该文档。局部补丁影响算法/诊断与测试合同，按实质规格修订处理，v2不冒充纯排版修改。修订时没有已完成的Stage05–08产物，故不存在要清零的已完成下游或需虚构的DOWNSTREAM_INVALIDATED事件；本轮Stage05直接对v2复核。原Stage04证据保留为v1历史，新增证据另记，Stage01–03不改。

## 3. Critical Findings Summary

审查对象中的大部分控制方程和FVM关系成立；未发现必须更换B/C物理模型的CRITICAL问题。最终归并为9项：CRITICAL 0、HIGH 3、MEDIUM 5、LOW 1；均已进入权威Stage04并经受影响复核关闭。严重度指修复前的缺口，不是剩余阻塞数量。§21逐项记录复核与仍待执行的验证。

| ID / 严重度 | 原位置及实际发现 | 依据、影响与最小修复 | 处置 |
| --- | --- | --- | --- |
| F01 HIGH | v1 §6.8“BE回退受同一误差监测”未明确回退监测式 | 若BDF2−BE差E>1导致拒步，复用该E不能批准同一步BE。明确回退在缩步后独立比较完整BE/两半BE，接受完整步；重试预算及历史一并明确 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F02 HIGH | v1 §16.2/17.2只有配置加严事件差，缺水分误差到时间的条件性检查 | 小eG遇小下降斜率可产生大时间偏差，0.01s括区间无补救作用。定位c*±ηstrict，纳入et；按场误差→余量→候选报告时刻→覆盖再评估顺序核验 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F03 HIGH | v1 §6.8线性“缩放残差≤10⁻⁸”未定义归一化，误差比较尺度可分叉 | 固定每轮线搜索尺度，给线性后向残差分母；明确归一化/原单位Jacobian关系和主候选误差尺度，防止非线性解错误验收 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F04 MEDIUM | v1 §17.2未使用连续达标保持性，数值短段与正式模型关系未充分区分 | 加权正部能量证明成立；保留简单数值区间交，正式重建反弹触发加严/未解析，不解释为连续模型现象 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F05 MEDIUM | v1 §16.4“C乘上轴向因子”可误乘背景常数 | 精确双重积分显示整个表达式相乘会改变Cb/Tb。将四个初始平均公式明确展开，仅扰动乘轴向因子 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F06 MEDIUM | v1 §6.7层余额未给与Newton容许残差的量化连接 | 原代数恒等式正确，补Σ权重×Newton残差上限及浮点约定，避免把容许代数误差误判为守恒失败；累计时间余额仍独立 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F07 MEDIUM | v1 §17.4检查点仅泛称“必要多步历史” | 明确两接受状态、步长/阶数/侧别、回退与事件覆盖记录；辅助解不入正式历史；BE恢复与最后行覆盖相容 | Stage04修复；CLOSED_SPEC_RECHECKED |
| F08 MEDIUM | v1及v2首轮§16.4只定义连续MMS源，未显式接入离散方程/诊断 | 明确B/C的源权重、SW/Sa和瞬时/累计/BDF层减源；避免正确有源制造解被无源余额误判失败 | Stage04修复；独立审计定向回读及含源符号检查通过 |
| F09 LOW | v1 §16.1将B/C重合轨迹及较晚事件延续混为共同统计窗口 | 主统计回到Stage03 §14.1的较早事件前共同区间，另标更晚展示/参考延续；补齐无事件的区间语义，原容差不变 | Stage04范围澄清；CLOSED_SPEC_RECHECKED |

F01/F03在独立审计中的责任阶段建议为RETURN_STAGE_04；父代理采纳其数学理由，并依据用户已授权“本轮局部修改Stage04再复核”执行该上游修复，不将它下放工程阶段。审计最终把归一化Newton缺口评为HIGH、历史检查点评为MEDIUM，父代理采纳，覆盖草稿中较低评级：二者分别可能改变求解验收和恢复轨迹。F02是关键诊断接口补全，不是改物理目标或换积分算法；Astra定向回读发现的余量/et/tend执行顺序歧义已在§17.2步骤4–5改正，并由父代理重新检查依赖顺序。F08按Sol意见补入后，其再次回读确认关闭；F09为父代理核对Stage03发现的范围澄清。

未判错的项目：变步BDF2系数和低阶差的1/2控制指数、M⁻¹b内状态相关a导数要求、F1/F2、径向/二维水余额、独立热时间导数路径、现有线性基节点区间交及最后行集合。它们按下节具体理由保留，不为形成修复数量而重写。

## 4. Global Symbol, Unit, and Domain QA

Stage04 §2/5.1的长度m、时间s、温度K、C干基kg/kg及显示℃/cm映射一致。扩散指数使用K；C是指定单位的数值，含水尺度和3850K使指数无量纲。Q1只D(C)非线性而热参数固定，Q2/3/4才有C→a,k以及T/C→D的耦合。内部温标平移不改变热差和导热式。

水PDE两侧为(kg/kg)/s；归一化通量jC单位为m/s乘kg/kg，真实水质量通量还需ρd。热PDE为W/m³，a单位J/(m³·K)。水边界D/R乘ξ导数与hm(C−Ceq)匹配；C的z保持米单位，轴向不附R⁻²。ρ只经a进入热方程，干质量参考权重独立，不反推未测绝对Md。

C≥0、T>0、R>0及C→0右极限已定义；正式接受状态必须正C并满足比较包络的数值检查。无任意D下限或clamp。原符号n同时用于法向/时间层、D用于扩散/来源标记，按上下文可区分，未产生未定义算子；无需全篇重命名。

## 5. Objective and Multi-Objective QA

这是给定输入的前向场与阈值首次进入问题，不是自由调温/调D缩短工期的优化。连续t*=inf{G<0.15}、数值t_critical_estimate和严格t_report分别有定义；g=0不是严格达标，开放可行集不必有最小元。无任意多目标权重、整数目标、补救决策或Big-M。

题面四位小数是显示要求；阶段研究容差不是物理精度承诺。Q2/Q3同一解源避免输出和事件各算各的。此项ACCEPT。

## 6. Constraint and Conservation QA

| 题意/既有承诺 | 权威公式/合同 | 审查结论 |
| --- | --- | --- |
| 四问参数族及初始状态 | §4、5.1、B1–B4 | 附录2/3/4映射明确；Q2/3从原初值统一采用附录3 |
| 轴心对称、表面热湿交换 | B2/B4、F1/F2、C3 | 方向/单位正确，允许热流入和吸湿，不强制Dirichlet |
| Q4自相似材料收缩 | §6.3、B3及R重建 | 相对输运项在该假设下抵消；F无直接Rdot，物理速度/诊断仍需Rdot |
| 单位干质量水余额 | B5/F4、C4/C7 | 内面共用且相消；二维侧面和端面系数正确 |
| 有效热余额与移动域修正 | B6/B7、C5及§6.7 | aDsT不同于d(aT)/dt；不据简化余额声称完整热力学 |
| 全域严格低于阈值 | §17.1/17.2 | 全节点极值＋时段区间交；补条件性和保持性 |
| 真实模板与最终行 | §17.3/17.4 | 固定物理距离、表面列、域外空白、末行去重和覆盖要求明确 |

FVM状态是体积加权单元平均。质心处物性评估、内部面距离加权调和和表面半单元常系数是显式求积近似，彼此并无必然矛盾；没有运行网格验证前不宣称其整体阶数。输出插值不逐单元维持平均约束，这已由v2明确为后处理，禁止用于重新初始化或W积分；分别检查平均误差和重建场误差。

F2在当前Newton试探状态更新AN，与最后控制体共享同一ps。δs是实际米距离；b=0、UN=Ue、δs→0、正系数下的凸组合及两种通量相等已经符号/分数检查。角点O2仅为后处理，侧/端面积独立计入，无角点新增源。

层余额ΨW、Ψa是加权Newton残差，验证装配及非线性收敛，不能证明时间积分精度。v2给出其τN、s、权重上限及浮点约定。独立累计余额用接受层线性时间路径的(Tn+1−Tn)/hn，不用PDE右端定义Tdot；在故意不满足热方程的孤立路径测试中得到非零残差271/300（人工尺度），能说明这一诊断表达式并非必然恒等于0。正式实现是否真的走该路径仍须Stage06/07检查。

## 7. Feasibility and Boundedness QA

不存在要证明非空的优化可行域；当前方程具有正初态、明确边界和分段输入，初始水分与Robin一阶不相容按弱/温和解初始层解释，保留题给初值。正半径、正a/k及正式正值区间D>0支持抛物型分析；D低C端可能极小，不能等同于数值条件良好。

正式比较包络与无源性支持有界场。新增阈值保持性用正部能量法证明：t0已全域低于c*时，取M=max(G(t0),0.05025)<c*，对w=(Ĉ−M)+作ξ加权测试，内部扩散与暴露Robin项均非正，其他边界为0，故随后C≤M。温度影响D不改变这个符号论证。完整式见Stage04 §17.2，覆盖B、C0/C1与固定/收缩几何。

该证明没有给出首次事件的存在时间，也不保证72h内达标；没有断言每个点都一直降低。正式输入下数值轨迹进入后退出是需要加严的异常，不作为真实反弹解释。超出已定义输入范围不能利用此证明外推结果。

## 8. Uncertainty, Scenario, and Recourse QA

S0/S1、两个邻近平台窗口、R线性/PCHIP及C0/C1均按固定规则定义，没有概率或任意参数分布。S0在14400s保留观测点值、右侧切均值；R_EXT_HOLD是明示条件延拓，不是72h以后的测量。默认Q4到输入覆盖终点未达标返回INPUT_HORIZON_REACHED；固定几何或已有延拓先到有限tmax返回TIME_LIMIT_REACHED。

本任务是离线重建，允许区间内双侧观测；并非在线决策漏用未来信息。无随机优化或recourse，不把情景比较称为鲁棒最优。上述解释保持原值，此项ACCEPT；场/时间敏感性为后续NOT_RUN。

## 9. Statistical, Estimation, and Identifiability QA

材料参数取自题面，S0的61点样本均值与PCHIP斜率由确定规则计算，没有未定义拟合目标。外部环境及R数据不能独立辨识内部D、hm、吸附等温线或被省略热机制；B通过固定经验闭合保持可实施，不能据此称物理参数已识别。

P的严格湿密度组合不相容风险仍保留，未用算力或数据插值“修复”它。独立内部T/C/质量资料缺失是验证限制，不是当前前向模型必然无法编码的规格缺陷。无正态/其他分布假设、统计置信区间或新辨识参数。

## 10. Structural and Convexity QA

B/C为确定性、状态系数非线性的抛物型前向系统与事件定位；Q4变换后为固定材料域上的非自治系统。无优化凸性、整数放松或KKT主张。NO BIG-M REQUIRED。

F=M⁻¹b可计算且状态依赖质量矩阵为正对角。Newton求归一化残差，JF=M⁻¹[b_y−M_yF]；等价原单位热残差对C确有a′D2T项。在非根处两种残差Jacobian不应混用，v2已写明，符号检查给出遗漏a′项的非零差异。原稿没有实际漏掉a′的错误，补丁只是防止工程误解。

## 11. Numerical Stability QA

### 11.1 BDF2与误差控制

变步系数在步长比1时为(3/2,−2,1/2)，对常数、一次和二次多项式精确，已用符号变量核验。比率r≤1.5时零方程寄生增量因子r²/(1+2r)≤9/16；只说明该零方程的历史控制，不泛化为非线性稳定性或正值保证。

BDF2/BE同终点差在光滑小步渐近下为O(h²)，1/2次方控制指数正确；孤立y′=λy表达式给其首项−λ²h²/2。完整BE与两个半BE的2倍差首项为λ²h²/2，可监测所接受完整BE的低阶误差。均不是Newton残差或严格BDF2局部误差界。正式时间精度仍靠独立加严，不将本轮轻量检查称作时间求解器通过。

F01修复明确所有BE主步均独立使用步长加倍监测；F03统一主候选尺度，冻结每轮线搜索尺度，给出线性后向残差和零分母规则。拒步不更新历史；辅助比较解不入正式轨迹；BE成功后用最近两接受状态恢复BDF2。Newton/时间差/状态有效性分别通过才接受。

### 11.2 输入节点与重启

真跳变、连续折点和纯输出三类已明确。主ODE没有直接Rdot，因此不以Rdot跳变为“必须重启”的依据。保留所有输入节点落步并BE重启是有意、保守的v1策略；成本为更多低阶步和启动开销，尚无阶次/性能证明。纯输出不重启，事件强制端点通常保留历史，重合输入节点只重启一次。

每类BE步、拒步和覆盖时间占比须记录；连续十个只能靠失败回退的接受步按原规则终止。实际执行身份为CUSTOM_VSBDF2_BE，不能长期BE却标成已验证二阶BDF2。

### 11.3 实际依赖与库身份

已读取本机两个解释器：用户Python 3.11.9含NumPy 2.4.4、SciPy 1.17.1、SymPy 1.14.0、openpyxl 3.1.5；本轮TEST_ONLY使用它。应用捆绑Python 3.12.14含NumPy/openpyxl，但没有SciPy/SymPy。未安装或升级任何依赖；不能因一个解释器缺包就宣称整台机器不可实施。

读取本机SciPy 1.17.1 `scipy.integrate.BDF.__doc__`确认：自动1–5阶、准恒步、NDF增强；`scipy.sparse.linalg.splu`已可导入，未运行积分器或线性求解。主方案仍为指定自定义BDF2，成熟库只能提供已区分的线性代数能力。两个官方网页入口的浏览器导航超时，未将网页内容记作已验证；算法身份依据本机同版本发行包随附文档，可在该版本官方页面另行核对，不为网页升级环境。

### 11.4 数值风险的去向

状态分量采用相对初温的温差缩放及C尺度；小D、刚性和非线性条件数仍需实际检查。试探无效状态回溯、最终状态拒步，禁止截断C、温度夹紧或任意D下限。线性后向误差小不自动表示解的前向误差小；未达到非线性条件时照样拒步。无未指定的数值保护参数由工程自行发明。

## 12. Simplification, Symmetry, and Redundancy QA

同一个B兼两角色只实现一次。C使用轴向半域、z=0中截面和z=ℓ实际端面；C0在轴向均匀状态下逐层退化为B，适用于固定与移动R。端面与侧面分别计面积，角点不增加质量/热量。增维只服务轴向误差参考，未重新引入P或完整流体模型。

O1/中截面外推算子可交换，张量插值权重非负。其轴点精确性不能扩张成整段精确；v2作了局部澄清，无需另造高阶、保守后处理体系而改变现有事件结构。

## 13. Cross-Route Fairness and Complexity-Premium QA

保留REF0_FIXED、REF0_MOVING、REF23、REF4。B/C同物性、初态、环境、半径情景、阈值协议和输出定义，分别把数值误差控制好后才讨论结构差；C径向展示取中截面，事件取整个二维域。不同网格规模不能当作相同数值精度。

结构容差原样保持：dT≤0.5K，dC/dG≤0.005kg/kg；临界事件差相对C临界时间≤1%且绝对≤1800s。C0退化为dT及T轴向离差≤0.02K，dC/dG及C轴向离差≤2×10⁻⁴kg/kg；每模型各量数值预算不超过对应容差10%。比较仍为d+eB+eC≤η才支持接受，d−eB−eC>η且自身合格才支持超标。

补充阈值扰动时间诊断不放宽上述标准。事件比较不能混用临界估计和严格报告时刻；缺事件不能用无穷或空值计算百分比。主共同场比较沿用Stage03至双方较早临界事件的区间，延续到较晚事件或指定论文时刻的部分另标参考延续，不偷偷改变主统计窗口。

## 14. Prediction-to-Decision and Error-Propagation QA

Q1不向Q2拼接末态，Q2/Q3同解，Q4同时换物性和几何，因此收缩归因须用附录4下固定R/变化R对照。输入情景改变、物性改变、维度差和数值误差不能合并归因为一个因素。

现有区间交针对固定基节点线性时间重建完整成立；它不是对真实接受节点间曲线的完整性证明。给定gtest=(θ−.30)(θ−.40)，端点/中点均正而中间有负区间，实际符号检查验证了有限采样的局限；这不否定现有指定线性重建上的证明。前序区间未解析时，后面事件不冠以全程最早。

F02加入c*±ηstrict的阈值扰动事件：et=max(et,ref,δt,lev)+括区间宽度。其本质为条件敏感性经验诊断；小斜率会放大水分误差，例如人工1e−5kg/kg与1e−8kg/kg/s对应1000s时间尺度，不能由0.01s根分辨率消除。没有已证误差界和真实下降下界，不称ε/m为严格保证。

t_report−t_critical_estimate分为余量/定位等待和显示向上取整两项，只有后者<0.36s。误差必须覆盖t_report，明显延后优先加严；预算不满足或下阈值范围内无事件保留未解析/候选状态，不靠无限等待制造完整结果。

## 15. Route B QA

数学有效性：在已声明有效闭合下，初边值、R变换、FVM、时间积分、误差/事件和输出有确定关系。F01–F09相关局部补丁已写入权威规格并复核，没有新增待辨识材料函数。

Route Readiness Status：`READY_FOR_IMPLEMENTATION`。技术上批准B按v2实施；用户尚未确认进入Stage06，故没有启动代码。物理适用性、实际误差及C参考结论仍未知。

## 16. Route P QA

章节合同中的Route P/Primary不能覆盖本题候选命名。Implementation Primary直接引用§15同一个B，不复制模型。历史内部P：`DEFERRED`，KEEP_IN_RESERVE，其严格湿密度解释和可识别性条件没有被本轮解除；不批准进入当前代码主链。

## 17. Route C QA

C的半域方程、轴向尺度、两方向FVM、端面C0/C1、整体余额和向B退化关系成立；与B共同的数值修复同样适用。MMS显式二维初始平均及含源余额已修复并复核。参考用中截面展示不缩小全域事件范围。

Route Readiness Status：`READY_FOR_IMPLEMENTATION`。技术批准范围只包括既定最小二维参考，未自动升级为正式主路线；执行仍待当前完整方案人审确认。

## 18. Other Route QA

NONE。H为DROP/不启用。输入敏感性与纯BE数值对照不是新增物理候选。

## 19. Validation Interface QA

Stage04 §16已有V01–V14测试：单位/物性、封闭均匀、无水交换、边界极限、固定/移动制造解、初始层、C0退化、独立余额、空间/时间/非线性/输出与事件加严、C1结构参考、输入/几何敏感性、事件压力及真实模板。制造解源项/边界经符号核验相容，修订初始平均后背景常数不被误乘；真实收敛阶仍NOT_RUN。

本轮最终执行48项轻量断言并全部通过，其中38项TEST_ONLY_EXECUTED、10项SPEC_RECHECKED。详见evidence中的逐项名称、观测值和脚本哈希：变步多项式、低阶差渐近、质量Jacobian关系、串联阻力、微小网格通量代数求和、MMS表达式与含源全局余额、移动体积Reynolds修正、故意失衡的热路径、孤立事件函数、人工末行时间集合与覆盖、原件哈希/模板以及依赖身份。新增源项检查实际做了单元权重求和和解析积分；小网格没有状态演化，没有求解任何真实或缩小版PDE。

V05已明确B/C源权重及SW/Sa：水和热瞬时/累计余额减源，BDF层减当前时刻hSW/hSa；V08正式模型继续无源。Sol已定向确认该接口闭合，父代理符号核验1D/2D、固定/移动半径含源余额；这些结果只证明公式关系，不代表制造解求解或收敛阶已经通过。

证据标签：`SPEC_RECHECKED`表示定义/源核对；`TEST_ONLY_EXECUTED`表示孤立检查确实执行；`FORMAL_NUMERICAL_VALIDATION_NOT_RUN`表示正式模型求解、数值收敛、效果和实证验证均未运行。没有内部场实測资料，V14为DATA_UNAVAILABLE/NOT_RUN。未将测试断言数量或Markdown格式当Gate的主要依据。

## 20. Implementation Interface QA

输入、物性、FVM、时间推进、重建、诊断、事件和导出接口已分开。运行配置必须记录模型/输入/网格版本、路线、参数族、环境/端面/半径情景、有限上限、精度、用途。主算法自定义，线性库可复用，不允许静默调用变阶BDF代替。

最后合法行形成闭环：Q2/Q3同一个正式解ID；每1s/60s集合之后必要时追加t_report，整格点不重复；向上报告格点先被同一接受解覆盖并复核，再交输出模块。Q4按行时刻的实际R判断域内外，域外空白、真0和数值失败分开，表面列取同一时刻ξ=1的重建。

流式块至多4096行/表，Excel上限1,048,576行/16,384列；没有实际事件时只做有限tmax资源上界预检，不声称最终文件大小/行数。超容量不截断、降采样或改表。存储检查点包含两接受状态与时间历史，禁止辅助解入轨迹；任何失败状态不能触发完整结果成功路径。

## 21. Pre-Implementation Repair List

| ID | 责任/权威位置 | 已实施修复 | 当前复核与剩余执行 |
| --- | --- | --- | --- |
| F01 HIGH | Stage04 §6.8 | BE回退独立误差监测、失败预算与接受历史 | CLOSED；符号检查及Sol回读通过；运行分支测试NOT_RUN |
| F02 HIGH | Stage04 §16.2/17.2 | 阈值扰动时间诊断、误差覆盖、预算及未解析状态 | CLOSED；Astra数学回读通过；随后执行顺序澄清由父代理复核；真实事件/收敛NOT_RUN |
| F03 HIGH | Stage04 §6.8 | 缩放线性残差、统一主候选尺度、Jacobian坐标 | CLOSED；代数关系及Sol回读通过；实际Newton/控制器NOT_RUN |
| F04 MEDIUM | Stage04 §17.2/16.5 | 连续保持性、数值反弹和完整性范围 | CLOSED；Astra/父代理正部能量证明复核；真实数值异常检查NOT_RUN |
| F05 MEDIUM | Stage04 §16.4 | MMS二维均值只让扰动乘轴向因子 | CLOSED；符号积分、反例及代理回读通过；MMS求解NOT_RUN |
| F06 MEDIUM | Stage04 §6.7 | 层余额与Newton容许误差关联 | CLOSED；加权残差等式及Sol回读通过；正式累计余额NOT_RUN |
| F07 MEDIUM | Stage04 §6.8/17.4 | 明确检查点历史、BE恢复及输出成功前提 | CLOSED；Sol回读及人工末行/去重/覆盖断言通过；恢复及Excel实际导出NOT_RUN |
| F08 MEDIUM | Stage04 §16.4 | MMS离散源和源修正余额 | CLOSED；Sol追加定向确认、六项新增代数/符号断言通过；MMS运行NOT_RUN |
| F09 LOW | Stage04 §16.1 | 主比较窗口与补充延续分开 | CLOSED；父代理逐条对照Stage03 §14.1；B/C真实比较NOT_RUN |

这些规格责任不留给Stage06临时决策。后续Stage06应实现并检验已定关系，Stage07评价真实返回结果。局部修复前有实施阻塞；关闭必须以v2受影响复核为准，不以“以后会测试”豁免错误。

## 22. Implementation Approval

**Gate05：CONDITIONAL_PASS。**当前CRITICAL/HIGH/MEDIUM数学实施缺口均已修复并复核，没有开放的实施阻塞。B和C均获技术实施批准；P保持DEFERRED，H保持DROP。条件是按§19/24完成后续真实执行验证，并保留有效闭合、长期情景及实测不足等限制；不能把这些尚未实证的问题称为已验证。

状态已按原schema登记Stage05和一个STAGE_05_COMPLETED事件；下一阶段建议06_CODE_ENGINEERING。Stage06–08仍NOT_RUN，后续产物指针仍null，current_best_verified=null、model_frozen=false。当前不创建工程、不运行正式模型。用户必须看过下面及最终回复中的A26-04-v2完整方案后明确确认；技术Gate不是该确认。

## 23. User Decisions Required

没有尚缺的目标权重、风险偏好或材料参数需要用户临时补定义；原经验闭合和情景已有本项目授权，保持其假设标签。唯一当前执行决策是：用户审核修订后A26-04-v2及本报告展示的方案，再决定是否进入Stage06。较长半径延拓在未来运行配置中必须显式指定，并不是默认授权无限延长。

用户已授权PERFORMANCE及有界子代理审计；未授权Stage06。本报告不将本轮Stage05授权、先前Stage04条件通过、用户沉默或技术Gate当作该确认。

## 24. Handoff to Stage 06

### Human-Review Modeling Plan

当前供审版本为Stage04 `A26-04-v2`，输入`A26-INPUT-v1`，选模仍为Stage03 v1。药材初始L=0.25m、R0=0.02m、T0=301.15K、C0=2.55kg/kg；附件1提供0–4h环境，附件2提供0–72h半径，四个真实模板约束输出格式。Q1采用附录2，Q2/Q3从原初值全程采用附录3并共用一条正式轨迹，Q4采用附录4和R(t)，不把各问末态拼接。

保留B一维径向有效输运作为主线兼基准，C作为轴向参考。B假设轴向/周向均匀、各向同性有效物性、均匀守恒干质量参考；题面ρ只进入a=ρcp的有效热系数，不强制解释为真实湿密度。气固交换采用固定单位斜率的有效Ceq映射；后问沿用h/hm、C1端面沿用侧面系数属于情景解释。省略潜热、迁移水焓和变形功。Q4假设长度不变和径向自相似运动，R由观测重建；这些物理假设没有在本轮被实证确认。

内部单位为m、s、K及干基kg/kg，待求T、C；目标是给定输入下的温湿场和全域严格C<0.15事件，没有可调环境优化量。主物理方程及边界为

$$
\widehat C_t=\frac{1}{R^2\xi}\partial_\xi(\xi D\widehat C_\xi),\qquad
a(\widehat C)\widehat T_t=\frac{1}{R^2\xi}\partial_\xi(\xi k\widehat T_\xi),
$$
$$
\widehat C_\xi(0)=\widehat T_\xi(0)=0,\quad
-\frac D R\widehat C_\xi(1)=h_m(C_s-C_{\rm eq}),\quad
-\frac k R\widehat T_\xi(1)=h(T_s-T_\infty).
$$

Q4用ur=rRdot/R使材料与网格运动一致，输运项相消；固定问题R=R0。C在z∈[0,L/2]增加正确的∂z(D∂zC)和∂z(k∂zT)项，C0端面无交换、C1端面Robin，中截面对称；其水量包含侧面和真实端面贡献。正式表的二维参考取中截面，但事件在全二维域检查。

求解算法与上述模型分开：使用圆环/轴对称FVM，单元平均、体积权重及距离加权调和内部面；表面用当前相邻单元系数的半单元串联阻力同时求Us、ps。MOL组成状态相关质量矩阵，按自定义变步BDF2同步Newton求解；所有BE主步以完整步/两半步检查，失败回退受限。输入真跳变和连续折点按有意保守策略落步重启，输出时刻不重启；所有实际阶数与BE占比据实记录，不用SciPy的变阶BDF冒名替代。

环境0–4h分段线性，S0之后保持3–4h的61点均值，S1保持末点；S0在4h明确小跳变。Q4 R采用分段线性，平台Rdot=0，默认到72h无尾部；若后来明确R_EXT_HOLD及有限tmax才做条件延拓。范围截止是停止原因，不是干燥结论。

水余额为W=2∫ξĈdξ及Wdot=−2js/R；热检查实际aDsT与边界热流，另作Reynolds修正，绝不以aT差或完整热力学宣传代替。离散层余额只检装配/Newton，独立累计余额用接受时间路径导数和独立求积。真实空间/时间/非线性/事件加严后控制误差；当前这些正式验证都NOT_RUN。

事件以全域重建最大值判断，包括真实轴心、表面、二维端面和角点。基节点在一个接受步中线性随时间变化，严格区间交完整覆盖该指定重建；连续模型另有达标保持性证明，数值反弹会触发加严。临界估计、0.01s定位括区间、含水率余量、阈值扰动时间敏感性及报告滞后分别记录。向上取0.36s显示格点后，必须在同一已覆盖解源上再验证Ĝ+ηstrict<0.15，不用显示成0.1500与否决定达标。

输出按Q1每秒至1800s、Q2每秒至同解Q3报告时刻、Q3/Q4每60s及必要末行；Q2事件较早时同解另延续至3h作论文展示。Q4固定物理距离域外留空，独立表面列取ξ=1；流式块与容量预检保证不遗漏最后合法行。失败/未解析只能形成相应状态记录，不能输出冒充完整干燥的正式结果。

验证优先级固定为：**基础输入与公式测试 → 面通量/边界及余额测试 → 固定几何小型验证 → 收缩和C0退化验证 → 正式分问求解与导出**。随后执行C1与B的同情景比较、S0/S1/窗口及R方法敏感性、附录4同物性几何对照。B/C各自先满足数值预算，再用0.5K、0.005kg/kg、1%且1800s的结构标准；C0采用更严格标准。缺少独立场与总质量数据，不能声称物理实证精度或最终最优/冻结。

### Human Feedback and Confirmation

| 用户消息/审查主张 | 证据与裁定 | 处置及复核 |
| --- | --- | --- |
| 本轮Stage05提示列出A–E重点，说明它们不是已证错误 | 正确的审查要求；实际阅读发现F01/F02等缺口，也确认多项原公式成立 | 保留正确项，只修权威Stage04局部，逐项记录证据 |
| “话说你怎么没有用子代理审计？这是正式比赛啊” | 新执行要求；此前ECONOMY及pool默认禁用不代表已做独立复审 | 启用一次Sol/High有界独立审计，返回内容与父代理裁定记录在证据 |
| “不要保持economy直接开启最高模式” | 明确模式变更要求，不是数学错误或Stage06授权 | 本项目改PERFORMANCE，新增Astra/Ultra核心复核；全局pool不重写、无递归代理 |
| 是否已经人工确认当前修订后的全方案并进入Stage06 | NOT_CONFIRMED；尚无对应消息或认可全文的证据 | 本报告及最终回复展示A26-04-v2；停在Stage05等待确认 |

技术Gate与人工确认分开。此处记录只使用现有报告和状态schema，不新建阶段或自动确认机制；本轮不执行任何Stage06工作。

GATE 05: CONDITIONAL PASS

STATUS: READY_FOR_STAGE06_HUMAN_REVIEW

### Stage06 Human Confirmation Update

2026-09-10T15:03:09.431660+00:00：用户明确确认以 A26-04-v2 作为 Stage06 实施基线；原 NOT_CONFIRMED 行保留为当时历史。已授权代码开发与服务器运行包交付。用户随后进一步明确不再本地真实运行，后续数值执行交服务器。本次为确认/执行边界更新，不修改 Stage05 数学结论或权威规格。
