# Q2：固定213候选的评分与协议分支实验

2026-09-11，起点9ea6187。用户已授权进行本地实验。本轮只回答：保存的15情景评分是否充分、排名是否稳定、near分支是否正确纳入。沿用Python和已有几何函数，不修改questions.py、geometry.py、Q3/Q4、MEC、原始结果或工作中的paper目录。

## Model Contract

主要指标为第二观测后的定位区域直径；同时报告第二动作时间T=移动/5+5、原有J=D+0.2T。λ=0.2只用于与原保存选择同口径比较，不重新校准；不把清除时间作为本轮目标。原213候选、顶点数组、角误差常数及旧CSV逐字节冻结，不新增站位。

1. 复算所有213点原15情景，与原CSV核对。记录原排名、分差、动作时间和接收余量。
2. 固定外包多边形，直接审计连续报告角z的代理F(s)=sup_z diam(Ω∩W(s,z,ε))。对72个5°初始区间，以中点原半宽计算采样下值，以半宽ε+区间半长的扩大wedge计算区间上值；优先二分最大上值区间。所有半宽保持小于90°，周期区间使用未回卷角度。每点目标上下差0.01m，最多20000次区间二分；保存1/0.1/0.01m进度与最终覆盖全周的叶区间，未达标如实记录。新评分的小多边形直径使用全部顶点两两距离，与原calipers在旧15情景逐一交叉核对；同时记录裁剪约束残差、子区间上值嵌套残差，不以clamp隐藏异常。
3. 独立构造首测(0,0)、30°相容的真源位置及有界误差情景。粗层为9个角×17个半径×5个误差，细层为17×33×9，网格嵌套；半径从5.01至1500−1e−9m，角从29+1e−10至31−1e−10°，误差从−1至1°。端点微向内仅防止浮点表达越出物理边界。第二报告角调用本地还原引擎的纯quantize_bearing函数，不实例化引擎、不调用接口、不构造随机噪声地图。量化后的首测也须能得到30°。
4. 每个原候选另加以它为中心、8个方向、距离0/4.999/5/5.001m的真源见证，仅保留与首测相容的位置。它们用于覆盖near边界，不增加候选站位。near按实际浮点距离≤5分支，不返回bearing；后验为Ω与5m盘交，报告直径上界min(10m,外切盘交的直径)。direction仍使用原wedge外包；不偷偷扣近场盘或改硬区域。

## 证据边界与停止规则

连续角的上下值只针对固定多边形的wedge代理。包含关系在精确几何中成立；实际使用原FP64裁剪，故称数值夹逼，不称严格浮点证书。它不等于真实协议连续最坏值的上下界：外包顶点可能非物理、近场不返回direction、报告角有量化等差异。若near可能，则全分支外包上值取max(direction上值,near_outer_upper)，其中near_outer_upper=min(10m,外切5m盘交直径)，不可漏掉near。

真源情景的最大值是有限情景的外包直径统计，不是已执行第二测量、真实位置误差、实际引擎种子成绩或连续最坏保证。首/次自由有界误差不保证能由同一个固定引擎噪声种子同时产生。粗细网格一致也不能替代误差界。每个候选使用同一公共真源网格；候选专属near见证单列，避免混淆共同情景比较。共同情景765+5049条/候选，213候选合计1,238,382条执行计数；粗细层嵌套重叠，不能称独立或唯一情景数。

本轮不采用新站位作为正式默认。若原排名稳定，报告其稳定范围；若改变，先区分代理采样不足、外包区域不对称和合法响应限制。候选加密、连续站位搜索、λ校准与clearance-aware另行留待证据充分后开展，不能混入本次效果。

## 冻结、计数与交付

运行前保存Git版本、源码及输入SHA256、软件版本、实验参数。结果写入新的J:/2026B_experiments/q2_score_20260911目录，213个候选全部完成，不只报告前几名。逐候选记录情景数、分支数、D与最坏D对应R、虚拟动作时间和实际计算时间；求解器/模拟器/官方运行均为0，几何后验重算数单列。

Figure Contract：一张三面板图分别展示全213点旧评分与连续角代理差异、前列候选数值夹逼区间、共同合法情景的粗细层变化。只证明固定算例下评分与排名稳定性，不推断泛化收益。输出PNG/SVG、CSV、JSON证据与中文结论，原始记录保留。
