# Evaluation preregistration supplement

Before evaluating outcomes: use belief_v2, a mathematical correction to finite mixture handling. If an unknown channel has zero surviving source particles, its absent hypothesis still has mass. With >=10 confirmed sources this can be a valid posterior; zero likelihood alone is not a global failure. Only zero joint evidence makes prediction unavailable. Zero conditional support is recorded; finite-particle zero does not certify physical absence and never removes mandatory nodes.

Power=17 scrambled Sobol, seed14001. Rank by expected_new, stable R12-order ties. Equal scene-weight primary estimates; all actual discovery decisions included, including states without remaining sources. MRR=0 if no candidate can discover a source. Uniform ranking integrated analytically. Uniform probability is mean geometry probability over available nodes (no fitted outcome prevalence). Distance/R12 are ranking comparators, no artificial Brier assigned. Bootstrap10000 paired scene resamples, seed14002. No subgroup selected after seeing results.

Truth labels generated through a separate Engine after both predictions. Its history and outputs never enter the belief. Labels describe a complete candidate-node scan for unknown channels, not a policy rollout or time benefit. Phase B/C still required.
