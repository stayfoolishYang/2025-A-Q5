# R13 preregistration revision 2

Audit anchor 344b455; frozen source 7228633. Official calls=0, formal tests=0.

Development128 (chosen before execution, retained from original plan), count-stratified N10..16 strict mixed native generator; fresh holdout256 only after candidate freeze. Existing 519 is regression only. Deduplicate accessible historical seed/physical manifests and archived scenes; unknown official truths cannot be certified disjoint. No synthetic edge/cluster strata are mixed into native random averages.

A1: deterministic multistart NN + converged 2-opt + relocate, retain actual frozen R12 incumbent at initial routing, after discovery and D1 refresh. No DP online. No A2 without measured construction dependence. Same mandatory node multiset asserted.
B-lite: only when the R12 best local is certified, compare certified clear actions and next discovery using immediate cost + remaining route length/5. Otherwise R12 unchanged. Discovery immediate scans use exact fixed-cleared scan sequence; unknown localization endpoints never guessed.
C1: R12 primary cost, certified scan credit only breaks numerical ties (1e-8); tie-free results justify STOP.
C2: same certified eligibility as B-lite, add exact future mandatory scan obligation after deterministic clear or discovery. No beta; clear leaves measurement channel unchanged.
D: only after known16 release, single optional support measurement targeting current best unresolved local channel. Compare direct min(existing diagnostic choose estimate, fallback proxy) against movement+measure+switch+weighted existing fallback estimate over existing diagnostic branches. Same deterministic1024 planning particle cap. Each optional point used at most once; known targets only; redecide after one measurement. Certified local cleared directly. Optional measurements tagged active_localization, never discovery.
E-H2: K=6 total actions (best local + up to5 nearest discovery nodes; six nodes if no local). H=2; frozen-information nominal rollout. Certified local ends at certificate point; ordinary one-step endpoint from Hypotheses.next with identical repeated-view adjustment. Diagnostic/fallback local endpoints unknown: use unchanged R12 decision at these states. Ordinary predicted measurement is charged once, then terminal current-local proxy (no claimed information gain); it cannot be repeated in the prefix. Discovery sweep uses exact present-cleared scans and endpoint/channel, no hypothetical new detections. Certified clear removes that track. Tail is remaining R12 open-route travel plus cheapest remaining local proxy, not a full-game expectation. No future scan tail in E-H2. No D/E combination in first selection. No H3/B-full/X by default.

Independent modules committed separately. Dev seven arms R12,A1,B-lite,C1,C2,D,E-H2. Eight CPU workers, one numerical thread each. Preserve failures, never silently overwrite results. Baseline copied run loop checked against frozen archived actions before batch. Hash source/config/scene in each run.

Selection: >=1% strong only if tails not materially worse; .3-1% retain with tail/CV/N16/compute benefit; <.3% STOP absent such benefit; mean improvement with >2% P95/P99 or >5% max deterioration RISKY. A stability benefit is >=2% P95/P99/max or CV reduction, or fewer N16 fallback runs. These operational definitions are fixed before results. Safety failure always REJECT. Choose <=3 complementary combinations only after dev, no B/C/E double counting. Holdout default >=1% improvement + safety + nonincreasing P95/P99 and <=5% max regression. .5-1% only STABILITY_CANDIDATE with all user-specified tail/stability benefits. Otherwise KEEP_R12. Bootstrap paired scene units 2000 reps seed20260912; N-stratified results and z=(T/N)/groupmean variance ddof1.

Route survival records uninterrupted discovery prefix only; localization, actual route replacement, known16, exit end segment. Node count, elapsed virtual time, executed prefix length/planned length reported. Do not sum snapshot gaps. Decision changes are not causal individual-action wins/losses: only whole paired scenes support outcome comparisons.

All terminal estimates are heuristics. Exact scan credits are conditional on unchanged mandatory nodes and fixed clear set; no claim of whole-game deterministic saving. Real decision deadline currently unverified; compute measurements cannot certify official real-time eligibility.
